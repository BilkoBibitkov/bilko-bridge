import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24 text-white">
      <h1 className="text-6xl font-extrabold text-center z-10 relative">
        Bilko lives
      </h1>
      {/* Any previous content from Ralph's era would typically be integrated here. */}
      {/* For this specific requirement, we focus on the central message. */}
    </main>
  );
}
